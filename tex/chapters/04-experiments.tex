\chapter{Auswertung}
In diesem Kapitel werden die Ansätze aus~\cref{chap:theoretischer_hintergrund} ausgewertet.
Die ersten beiden Experimente testen, ob die Solver bei gleichen Bedingungen die gleichen Ergebnisse liefern.
Die beiden Experimente danach werden genutzt, um den besten SAT-Solver für das gradbeschränkte Triangulierungsproblem aus~\cref{sec:pysat} zu finden.
Danach folgt in~\cref{sec:eval1:grund_solver} ein Experiment, welches die verschiedenen Solver mit den verschiedenen Beschränkungen vergleicht, um den besten Solver zu bestimmen.
Dieser wird genutzt, um in den folgenden Experimenten zum einen die theoretische Ausschließung von Kanten zu testen, Ja-Instanzen und Nein-Instanzen getrennt voneinander zu betrachten und möglichst große Instanzen zu lösen.
Anschließend werden in~\cref{sec:eval11} die Zielfunktionen betrachtet.
In dem darauf folgenden Experiment wird der CDCL-Ansatz aus~\cref{sec:cdcl} genauer betrachtet, um während des Lösens Einfluss auf den Solver zu nehmen.
Zum Abschluss wird in drei verschiedenen Experimenten versucht, die Schwierigkeit der Instanzen zu bestimmen.


\section{Versuchsumgebung}
\phillip{ Ist sowas wie Pandas relevant? Das wird doch nur in der Auswertung gebraucht...,}
\fabian{ Dann ist Slurminade und Algbench doch genauso unrelevant oder?, oder das die Plots mit plt und Seaborn erstellt wurden?}
% \begin{itemize}
%     \item Implementation details (e.g., libraries, third-party programs, \dots)
%     \item In which environment are you testing (e.g., system specifications)
%     \item What are you testing? (e.g., runtime, space needed, fps, \dots)
%     \item What are the results? Show fancy figures!
%     \item What does the results mean? (e.g., give a little discussion on your results)
% \end{itemize}

\begin{itemize}
    \item Algbench und Slurminade, Pandas
    \item plt und Seaborn
    \item Alles in python Teileweise Algos in c++ ausgelagert mit pybind11
    \item AMD Ryzen 7 5800X @ 8x 3.8GHz-4.7GHz, 128GB RAM
    \item Timeout von 5 min (300 Sekunden)
    \item Jede Instanze Generiungs möglichkeit (5 aufzählen)
    \begin{itemize}
        \item zwei normale
        \item eine move
        \item eine change
        \item größe 30 bis 120 in 10er Schritten
        \item Jeder Run wird 5 mal durchgeführt und der Durchschnitt genommen wenn nicht anders gesagt
        \item Da Permuation anders und Laufzeit bei mehrmals vorallem bei OrTools auch anders
    \end{itemize}
    \item Gesamte Laufzeit dargestellt Model konstruktion plus solve zeit
\end{itemize}

\section{Permutation}
\label{sec:eval2:permuation}
% (Eval2)
In diesem Experiment wird untersucht, ob es einen Einfluss hat, in welcher Reihenfolge die Knoten den Solvern übergeben werden.
Es wurden Instanzen mit 60 bis 90 Knoten betrachtet.
Als Beschränkungen kamen die Überschneidungsbeschränkung und die Gradbeschränkung zum Einsatz.
Wie in~\cref{fig:eval2:gurobi},~\cref{fig:eval2:ortools} und~\cref{fig:eval2:sat} zu erkennen ist, unterscheiden sich die Laufzeiten bei allen Solvern.
Daraus folgt, dass die Permutation der Knotenmenge einen Einfluss auf die Laufzeit hat.
In den folgenden Experimenten wird für jeden Lauf die Knotenmenge fünfmal in unterschiedlichen Permutationen übergeben und der Durchschnitt berechnet.

\section{Mehrmalige Durchläufe}
% (Eval7) 
In diesem Experiment wird untersucht, ob sich bei mehrfachen Durchläufen derselben Instanz mit demselben Solver und gleichen Parametern sich die Laufzeit verändert.
Auch hier wurden Instanzen mit 60 bis 90 Knoten betrachtet.
Als Beschränkungen kamen die Überschneidungsbeschränkung und die Gradbeschränkung zum Einsatz.
In~\cref{fig:eval7:gurobi} und~\cref{fig:eval7:sat} ist zu erkennen, dass alle Durchläufe die gleichen Laufzeiten aufweisen.
Nur bei OrTools ist in~\cref{fig:eval7:ortools} zu sehen, dass die Laufzeiten unterschiedlich sind.
Da aufgrund von~\cref{sec:eval2:permuation} bereits mehrere Durchläufe mit unterschiedlichen Permutationen durchgeführt werden, werden die OrTools-Durchläufe nicht zusätzlich mehrfach ausgeführt.

\section{SAT Gradbeschränkung}
% (Eval5)
In diesem Experiment werden Verschiedene Methoden der Gradbeschränkung in einem SAT Solver vergleichen.
Hierbei wurden für alle drei Methoden, also \emph{subset}, Kardinalität mit exaktem Sollgrad und Kardinalität mit Mindest Sollgrad, die Laufzeiten gemessen.
Bei den Kardinalitäts Codierugen von PySat wurden auch alle möglichen Codierungen Getestet.
Das Ziel ist es die Gradbeschränkung zu finden mit der besten Laufzeit, welche in den Folgenden Experimenten verwendet wird.
Für die Knotengröße wurden Instanzen der Größe 30 bis 90 verwendet.
Bei der \emph{subset} Methode wurde nur die Instanz mit 30 Knoten verwendet. 
Da bei größeren Knotenmengen der Arbeitsspeicher zu klein war.
Als zusätzliche Bedingung wurde die Überschneidungsbeschränkung verwendet.
Für die Exakte Methode sieht man in~\cref{fig:eval5_exact} das \emph{sequential counters} die besten Ergebnisse erzielen.
Auch für den Mindest Sollgrad ist \emph{sequential counters} die beste Variante wie man in~\cref{fig:eval5_atleast} sieht.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{eval5/gesamt.pdf}
    \caption[Vergleich von Kardinalitätscodierungen für SAT-Solver]{In der Abbildung werden für alle Instanzen Arten die drei Kardinalitätsbeschränungen vergliechen. Die \emph{subset} Methode ist die schlechteste, da sie nur für kleine Instanzen funktioniert. Die anderen beiden Methoden unterscheiden sich nicht signifikant.}
    \label{fig:eval5_gesamt}
\end{figure}


Der Vergleich der drei Methoden ist in~\cref{fig:eval5_gesamt} zu sehen.
Hier ist klar erkennbar das \emph{subset} die schlechteste Methode ist.
Das lässt sich leicht daran erklären das nur die kleinsten Instanzgrößen gelöst werden konnten.
Die anderen beiden Methoden unterscheiden sich nicht signifikant.
Es ist zu sehen das öfter die Methode mit dem exakten Sollgrad langsamer ist.
Dementsprechend werden in folgenden Experimenten die Kardinalitätsbeschränkung mit Mindest Sollgrad und \emph{sequential counters} Codierung verwendet.


\section{SAT Solver Vergleich}
% (Eval3)
In diesem Experiment werden die verschiedenen SAT Solver verglichen, die von PySat bereitgestellt werden.
PySat stellt \emph{Cadical195}, \emph{Gluecard4}, \emph{Glucose42}, \emph{Lingeling}, \emph{MapleChrono}, \emph{MapleCM}, \emph{Maplesat}, \emph{Mergesat3}, \emph{Minicard} und \emph{Minisat22} zur Verfügung. 
Von diesen wurden jeweils die neuesten verfügbaren Versionen verwendet.
Bei \emph{Gluecard4} und \emph{Minicard} kam anstelle von \emph{sequential counters} eine native Codierung der Gradbeschränkung zum Einsatz.
Alle anderen Solver nutzen die \emph{sequential counters} Codierung sowie die Überschneidungsbeschränkung. 
Der \emph{Lingeling} Solver wurde in großen Experimenten nicht berücksichtigt da er kein Timeout unterstützt.
Er wurde aber zusätzlich mit dem besten Solver vergleichen und hat schlechtere Ergebnisse erzielt.
Weitere Bedingungen wurden nicht berücksichtigt.
\emph{CryptoMinisat} wurde nicht verwendet, da dieser Solver speziell für XOR-Probleme optimiert ist.
Für die Instanzengröße wurden 60 bis 90 Knoten gewählt.
Das Ziel besteht darin, den Solver zu identifizieren, der die besten Ergebnisse liefert, um diesen in zukünftigen Experimenten einzusetzen.

\section{Grund Solver}
% (Eval1)
\label{sec:eval1:grund_solver}
In diesem Experiment werden alle möglichen Bedingungen an die Solver übergeben und einzeln getestet.
Untersucht werden die Solver SAT, OrTools und Gurobi jeweils mit der Kantenformulierung sowie der Dreiecksformulierung.
Für alle Durchläufe wurden die Überschneidungsbeschränkung und die Gradbeschränkung genutzt.
Bei der Kantenformulierung wurden das Fixieren und Ausschließen von Kanten sowie die \emph{alternative Kantenformulierung} getestet.
Bei der Dreiecksformulierung wurde nur das Ausschließen von Kanten getestet.
Die Instanzengröße liegt bei 30 bis 120 Knoten.

\begin{figure}[htb]
    \centering
    \includegraphics[width=1\linewidth]{figures/eval1/gesamt.pdf}
    \caption{Eval1 Gesamt}
    \label{fig:eval1:gesamt}
\end{figure}

\fabian{Finale Bilder analysieren}

\section{Kanten vorab berechnen}
% (Eval6)
In diesem Experiment wird die theoretische Ausschließung und Fixierung von Kanten untersucht.
Für eine Auswahl von Instanzen wird vor dem Experiment eine Menge von Kanten bestimmt, die sicher ausgeschlossen oder fixiert werden können.
Dies basiert auf der theoretischen Beschränkung aus~\cref{sec:exclude_edges:theoretisch_ausschließen}.
Die Grundlage für dieses Experiment ist die Annahme, dass das Ausschließen und Fixieren von Kanten in~\cref{sec:exclude_edges:theoretisch_ausschließen} nur schlecht performed hat, weil zu wenige Kanten ausgeschlossen wurden.
In diesem Experiment wurde ausschließlich der SAT Solver verwendet, da SAT und OrTools in~\cref{fig:eval1:gesamt} vergleichbare Ergebnisse liefern und dadurch die Übersichtlichkeit gewahrt bleibt.
Als weitere Beschränkungen kamen nur die Überschneidungsbeschränkung und die Gradbeschränkung zum Einsatz.
Für die Knotengröße wurden Instanzen mit 80 bis 120 Knoten gewählt.

\fabian{Finale Bilder analysieren}

\section{Ja/Nein getrennt}
% Eval12
In diesem Experiment wird untersucht, ob der SAT Solver bei Ja-Instanzen oder bei Nein-Instanzen schneller ist.
Es wird nur der SAT Solver mit den besten Parametern aus~\cref{sec:eval1:grund_solver} verwendet.
Die Knotenzahl liegt zwischen 80 und 120.
\fabian{Finale Bilder analysieren}

\section{Best Solver}
% (Eval13)
In diesem Experiment wird geprüft, welche Instanzen der Solver lösen kann, wenn das Zeitlimit von 5 Minuten auf 30 Minuten erhöht wird.
Dafür wurden Instanzen mit zunehmender Größe generiert, bis der Solver diese nicht mehr lösen konnte.
Die Knotenzahl lag am Ende zwischen 130 und 200.
Es wird erneut der beste Solver aus~\cref{sec:eval1:grund_solver} verwendet.
\fabian{Finale Bilder analysieren}


\section{Zielfunktion}
\label{sec:eval11}
% (Eval11)
\fabian{@Michael du meintest ich soll hier die Bound mit im Diagramm anzeigen. Ich weiß grade nicht wie ich das machen soll da er auf einer Achse mit Prozent nicht sinnvoll angezeigt werden kann}
\fabian{Wie soll ich die Diagramme aufbauen? Ich habe akutell Nur Greedy 120 Knoten instanzen und dann jetzt alle vier als eigene Diagramme.}
In diesem Experiment werden die drei verschiedenen Optimierungsansätze vergleichen.
Erste Ansatz ist der Flips Ansatz, die anderen beiden Ansätze sind die Zielfunktionen von OrTools

Der Flips Ansatz schafft Instanzen mit Maximal 8 Knoten. 
Dementsprechend wird er im weitern nicht betrachtet.
Alle anderen Druchläufe in diesem Experiment wurden mit 120 Knoten durchgeführt.

Im folgenden werden Ja-Instanzen und Nein-Instanzen getrennt betrachtet.
Bei Ja-Instenzen ist interesannt wie schnell die Instanze gelöst werden kann und ab wann gute Ergebnisse erzielt werden.
Bei den Nein-Intanzen ist hingegen interesannter wie nah der Solver an eine Lösung kommt.

\begin{figure}[htb]
    \centering
    \includegraphics[width=1\linewidth]{eval11/eval_progression_38.pdf}
    \caption{eval11 move}
    \label{fig:eval11:move}
\end{figure}

\begin{itemize}
    \item nur greedy betrachtet
    \item Min Max bei Ja besser
    \item Durchschnitt bei Nein besser
\end{itemize}

\cref{fig:eval11:36}
\cref{fig:eval11:37}
\cref{fig:eval11:move}
\cref{fig:eval11:change}

\section{\emph{Propagation} CDCL}
Der CDCL Solver namens \emph{CaDiCaL}~\cite{cadical} von Armin Biere ermöglicht es, eine neue Entscheidungsebene mit einer eigenen Funktion zu erzeugen.
Diese Funktion kann dann das nächste Literal setzen.
Im Folgenden wird diese Funktion \emph{Propagation} genannt.
Die Idee bei dieser \emph{Propagation} ist es, die Bedingungen aus~\cref{sec:exclude_edges} auf Teilinstanzen anzuwenden.

\subsection{Visualisierung}
Der erste Schritt war es, die Teilinstanzen, mit denen die \emph{Propagation} aufgerufen wird, zu visualisieren.
\begin{figure}[htb]
    \centering
    \includegraphics[width=1\linewidth]{cdcl/propergation.pdf}
    \caption{Eine Teilinstanz bei einem \emph{Propagation} Aufruf. Blau auf false gesetzt, grün auf true. Grüner Knoten Sollgrad erfüllt. Zahl in Knoten ist Sollgrad.
    TODO schön aufschreiben mit finalem Bild und erklären das von rand ausgehend fertig}
    \label{fig:cdcl:propagation}
\end{figure}

In~\cref{fig:cdcl:propagation} ist eine beispielhafte Teilinstanz dargestellt.
Bei der Visualisierung wurde festgestellt, dass es häufig zwei Arten von Teilinstanzen gibt.
Die erste Variante bildet ein Konstrukt, das eine Verbindung zum Rand besitzt.
In diesem Konstrukt ist meist eine gültige gradbeschränkte Triangulierung vorhanden.
Die zweite Variante besteht aus einzelnen Kanten, die isoliert in einer Fläche liegen.

\subsection{Implementierung}

Die Idee der \emph{Propagation} besteht darin, die in~\cref{sec:exclude_edges} beschriebenen Bedingungen zum Halbieren der Instanz anzuwenden.
Die \emph{Propagation} ist dabei insbesondere für die erste Variante der Teilinstanzen optimiert.
Der Vorteil dieser Teilinstanzen ist, dass die gesamte Instanz bereits teilweise halbiert ist.
Daher können gezielt Kanten getestet werden, die diese Halbierung vervollständigen.

Es wird angenommen, dass die Hülle fixiert wurde und alle Kanten, die die gesamte Instanz halbieren, bereits getestet wurden.
Der Grund hierfür ist, dass die Hülle eine geeignete Fläche bietet, mit der alle Punkte betrachtet werden können.
% Alle möglichen Kanten werden vor der \emph{Propagation} ausgeschlossen, damit sie durch Konflikte und Backtracking nicht mehrfach ausgeschlossen werden können.

Zur Umsetzung wird mithilfe eines Arrangements verfolgt, welche Kanten aktuell aktiv sind.
Es ist darauf zu achten, dass bei jeder neuen Entscheidungsebene das Arrangement gespeichert und eindeutig einer Ebene zugeordnet wird.
Tritt ein Konflikt auf, wird das Arrangement auf die entsprechende Entscheidungsebene zurückgesetzt.

Wird die \emph{Propagation} aufgerufen, können alle Flächen des Arrangements betrachtet werden.
Relevant sind dabei nur Flächen, die mehr als drei Kanten besitzen.
Zusätzlich kann die unbegrenzte Fläche ausgeschlossen werden, da alle Punkte durch die Hülle eingeschlossen sind.
Für diese Flächen können dann alle teilenden Kanten erzeugt werden, indem sämtliche Zweierkombinationen der Randknoten betrachtet werden.
Dadurch wird die Fläche in zwei Hälften geteilt und die Kante kann getestet werden.

Vor dem Testen können alle Kanten übersprungen werden, die bereits festgelegt wurden oder keine gültigen Kanten sind.
Für die Punktzuordnung kann zu Beginn jeder \emph{Propagation} jeder Punkt einer Fläche zugeordnet werden.
Sollte dann eine Kante getestet werden, können alle Punkte der zugehörigen Fläche betrachtet und ähnlich wie in~\cref{sec:intersection_edges} mithilfe von \emph{Rechtsknicks} und \emph{Linksknicks} einer Teilhälfte zugeordnet werden.

Führt das Testen einer Kante zu einem Konflikt, kann das zugehörige Literal \(l\) auf \emph{falsch} gesetzt werden. Als Begründung kann folgende Klausel verwendet werden
\[
(\overline{l} \lor \bigvee_{e \in E_A}\overline{x_e})
\]
wobei \(E_A\) die Menge der aktiven Kanten bezeichnet.

\subsection{Experiment}
TODO Ich habe den Propergate naiv implemenitert allerdings läuft er noch nicht ganz.
Ich würde hier beschreiben wie viele und welche Kanten er finden konnte da ich für einen Ansatz der Zeitlich relevant ist keine Zeit mehr habe.

\section{Instanzen Schwierigkeit}
In diesem Abschnitt wird Probiert die Schwierigkeit der Instanzen zu bestimmen.
Aus den vorigen Experimenten ist bekannt das \emph{Delaunay} und \emph{Delaunay Flips} die schwereren Instanzen sind,\emph{Greedy} im Mittelfeld und \emph{Iterativ} und \emph{Random}die einfacheren Instanzen.
Im ersten Teil wird geschaut ob die Anzahl der Lösungen pro Instanze einen Einfluss auf die Schwierigkeit haben. 
Danach im zweiten Teil wird geschaut wie der Grad verteilt ist.
Also wie oft jeder Grad vertreten wird.
Im Letzen Teil wird geschaut ob es einen Zusammenhang mit den Kantenlängen und der Schwierigkeit gibt.
Für alle drei Teile werden Instanzen der größe 30 bis 120 Knoten betrachtet.
\subsection{Anzahl Lösungen}
\label{sec:eval8:anzahl_lösungen}
% (Eval8)
\begin{figure}[htb]
    \centering
    \includegraphics[width=0.48\linewidth]{eval8/delaunay_008_delaunay_50_0.pdf}\hfill
    \includegraphics[width=0.48\linewidth]{eval8/delaunay_008_delaunay_50_1.pdf}
    \caption[zwei verschiedene Lösungen für eine Instanz]{In dem Bild sind zwei verschiedene Lösungen für eine Instanz zu sehen. In Grün sind die Kanten zu sehen welche in der Instanz unterschiedlich sind. In Blau die gleichen Kanten. Die Zahlen in den Knoten geben den Sollgrad an. }
\end{figure}
In~\cref{tab:triangulation_results} ist zu sehen das fast alle Instanzen nur eine Lösungen haben.
Vorallem ist aber zu sehen das es keinen Zusammenhang zwischen der Anzahl der Lösungen und der Instanze gibt.
Es kann also davon ausgegangen werden das die Anzahl der Lösungen keinen Einfluss auf die Schwierigkeit der Instanzen hat.
Interesannter weiße ist zu sehen das die Kanten welche in einer Instanze unterschiedliche sind klar einem Bereich zugeordnet werden könne.
Wie man in~\cref{fig:eval8:left} sieht, sind die Kanten in der linken Hälfte der Instanz unterschiedlich, während die rechte Hälfte keine Unterschiede aufweist.

\subsection{Grad Verteilung}
In diesem Experiment wird Probiert anhand der Gradverteilung der Knoten zu erkennen ob es einen Zusammenhang zwischen der Schwierigkeit und dem Grad gibt.
Dafür werden die Ja-Instanzen der Größe 30 bis 120 Knoten betrachtet.
% (Eval9)
\begin{figure}[htb]
    \centering
    \includegraphics[width=1\linewidth]{gradverteilung.pdf}
    \caption{Eval8 Anzahl Lösungen Greedy}
    \label{fig:eval9:gradverteilung}
\end{figure}
In~\cref{fig:eval9:gradverteilung} ist die Gradverteilung der Knoten zu sehen.
Auf den ersten Blick lässt sich vermuten das es einen Zusammenhang zwischen der Schwierigkeit und dem Grad gibt.
Es ist zu erkennen das Vorallem Random und Iterativ eine Rechtsschiefe Verteilung haben.
Dazu passend hat Delaunay eher eine zentrierte Verteilung. 
Auch das Delaunay-Flips Rechtsschiefer als Delaunay ist würde passen, da Delaunay-Flips einfacher als Delaunay sind.
Leider lässt sich an Greedy erkennen das er keinen direkten Zusammenhang zwischen der Gradverteilung und der Schwierigkeit gibt.
Greedy ist von der Zentrierung zwischen Delaunay und Delaunay-Flips einzuordnen aber die Greedy Instanzen sind eindeutig einfacher als Delaunay-Flips.
TODO anpassen greedy ist warscheinlich schwierig


\subsection{Kantenlängen Verteilung}
% (Eval10)
In diesem Experiment wird ebenfalls probiert einen Zusammenhang zwischen der Schwierigkeit und einer Eigenschaft zu finden.
In diesem Fall werden die Kantenlängen betrachtet.
Allerdings müssen diese Normiert werden da sonst durch weit auseinander liegende Knoten die Verteilung verzerren würden.
Die Normierung erfolgt durch das Teilen durch die maximale Kantenlänge.
Das heißt für jede Instanze wird die maximale Kantenlänge bestimmt und alle Kantenlängen durch diese geteilt.
Für dieses Experiment wurden nur Ja-Instanzen der größe 30 bis 80 Knoten betrachtet.
\begin{figure}[htb]
    \centering
    \includegraphics[width=1\linewidth]{edge_length_distribution_plots.pdf}
    \caption{Eval8 Anzahl Lösungen Greedy}
    \label{fig:eval10:kantenverteilung}
\end{figure}

TODOVLt ist greedy doch schwerer als gedacht.

